#session
  - if user_signed_in?
    = raw "Logged in as #{link_to(current_user, edit_user_registration_path)}. #{link_to("Log Out.", destroy_user_session_path)}"
  - else
    = link_to "Log In", new_user_session_path
