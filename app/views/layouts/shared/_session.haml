#session
  - if user_signed_in?
    = raw "Logged in as #{current_user}. #{link_to("Log Out", destroy_user_session_path)} | #{link_to "My Account", edit_user_registration_path}"
  - else
    = link_to "Log In", new_user_session_path
