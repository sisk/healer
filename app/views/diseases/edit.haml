- content_for :title do
  = "Edit #{@disease}"
- content_for :subnavigation do
  %ul
    %li= link_to("Delete record for #{@disease}", disease_path(@disease), :method => :delete, :confirm => "Are you sure? This may have rippling effects and cannot be undone.")
= render :partial => "form"
