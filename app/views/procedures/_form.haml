- semantic_form_for @procedure do |f|
  - f.inputs do
    = f.input :base_name, :as => :string, :label => 'Name'
    = f.input :code, :as => :string, :label => 'ICD-9 Code'
  - f.buttons do
    = f.commit_button