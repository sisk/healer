- semantic_form_for @body_part do |f|
  - f.inputs do
    = f.input :name, :as => :string, :label => 'Name'
    = f.input :side, :as => :radio, :label => "Side", :collection => [["Left", "L"], ["Right", "R"], ["N/A", ""]]
  - f.buttons do
    = f.commit_button